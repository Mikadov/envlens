name: Commitlint

on:
  pull_request:
    types: [opened, edited, synchronize]

permissions:
  pull-requests: read

jobs:
  pr-title:
    name: Validate PR title
    runs-on: ubuntu-latest
    steps:
      - uses: amannn/action-semantic-pull-request@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          # Enforce Conventional Commits types that Release Please understands.
          types: |
            feat
            fix
            chore
            docs
            test
            ci
            refactor
            perf
            build
            revert
          requireScope: false
          # Accept either case so Dependabot's "Bump X from Y to Z" titles pass.
          subjectPattern: ^[A-Za-z].+[^.]$
          subjectPatternError: |
            The subject must start with a letter and not end with a period.
